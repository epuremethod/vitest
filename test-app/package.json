{
  "name": "test-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "pretest": "pnpm --filter @epure/vitest build && rescript",
    "test": "vitest run",
    "test:watch": "vitest",
    "res:build": "rescript",
    "res:clean": "rescript clean",
    "res:dev": "rescript watch"
  },
  "devDependencies": {
    "tilia": "5.0.0-beta.20251218T180640",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.1.3",
    "@epure/vitest": "workspace:*"
  },
  "dependencies": {
    "rescript": "12.2.0"
  }
}
